{
  "type": "object",
  "properties": {
    "searchType": {
      "type": "string",
      "enum": ["nickname", "phone"]
    },
    "search": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "enum": ["register", "gifticon", "mission", "draw", "roulette", "etc", "today", "withdraw", "transfer", "expired"]
    },
    "status": {
      "type": "string",
      "enum": ["accumulate", "deduction"]
    },
    "startAt": {
      "type": "string",
      "format": "date"
    },
    "endAt": {
      "type": "string",
      "format": "date"
    },
    "start": {
      "type": "integer",
      "minimum": 0,
      "default:": 0
    },
    "perPage": {
      "type": "integer",
      "minimum": 5,
      "maximum": 1000,
      "default:": 25
    }
  },
  "required": [
    "start",
    "perPage"
  ],
  "additionalProperties": false
}
