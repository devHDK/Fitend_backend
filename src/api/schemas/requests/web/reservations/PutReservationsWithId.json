{
  "type": "object",
  "properties": {
    "startTime": {
      "type": "string",
      "format": "date-time"
    },
    "endTime": {
      "type": "string",
      "format": "date-time"
    },
    "status": {
      "type": "string",
      "enum": ["complete", "attendance", "cancel", "noShow"]
    }
  },
  "required": [
    "ticketId",
    "startTime",
    "status"
  ],
  "additionalProperties": false
}
