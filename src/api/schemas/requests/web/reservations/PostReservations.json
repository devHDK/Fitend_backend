{
  "type": "object",
  "properties": {
    "reservations": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "startTime": {
            "type": "string",
            "format": "date-time"
          },
          "endTime": {
            "type": "string",
            "format": "date-time"
          }
        },
        "required": [
          "startTime",
          "endTime"
        ]
      }
    },
    "ticketId": {
      "type": "integer"
    }
  },
  "required": [
    "reservations",
    "ticketId"
  ],
  "additionalProperties": false
}
